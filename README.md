phase-o-gps-1
=============

Commands:

git clone
git push
git pull
git status
git checkout-b
git checkout --help
git checkout
git add [filename]
git commit -m "insert commit message"


cd ..
ls
mkdir
